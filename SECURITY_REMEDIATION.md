# KICKS CENTER POS 資安修正清單

建立日期：2026-08-28  
狀態：資安修補已套用，待正式站部署驗證  
範圍：KICKS CENTER POS 正式站、Supabase、Vercel、管理員登入、供應商獨立頁面

## P0：修正前不應加入更多真實敏感資料

- [x] 關閉 Supabase 公開註冊，僅允許管理員建立或邀請帳號。
- [x] 建立明確角色：`admin`、`staff`、`vendor`，授權資料放在不可由使用者自行修改的獨立成員表。
- [x] 將 `kc_pos_state` 改為管理員／員工白名單，禁止一般登入者讀取或覆寫整包 POS 資料。
- [x] 修正 `orders`、`product_variants` 的寬鬆讀寫規則，全部改為 staff 白名單。
- [ ] Supabase 外洩密碼防護僅提供 Pro 方案；Free 方案目前無法啟用。已開啟安全密碼變更、要求目前密碼、最嚴格字元組合並關閉公開註冊。

## 供應商獨立登入與資料隔離

目前 `/vendors/[id]` 不是供應商專屬登入入口，而是共用 POS 登入後的內部頁面。頁面會先載入完整 `kc_pos_state`，再於瀏覽器依 URL 的 `vendorId` 篩選，因此只要帳號能登入，就可能取得所有供應商、庫存、回價、售價、銷售與結款資料。只隱藏畫面或限制 URL 不構成安全授權。

- [x] 供應商帳號必須綁定唯一 `vendor_id`，不得由網址、表單或使用者 metadata 自行指定授權對象。
- [x] 供應商入口與員工 POS 分流，供應商不得看到員工 Sidebar、商品入庫、掃碼銷售、銷帳操作或其他廠商資料。
- [x] 資料庫 RPC 依登入者綁定的 `vendor_id` 過濾，不會將完整 POS 資料下載至供應商瀏覽器。
- [x] 供應商入口預設只讀，沒有正式資料寫入權限。
- [x] 已以交易回滾測試變更身分／網址不可取得其他廠商資料。
- [x] 供應商快照不包含其他廠商、店家毛利、付款方式、庫存位置與內部備註。

## 資料完整性、稽核與備份

- [x] 在任何權限或資料結構變更前，匯出並校驗一份 Supabase 之外的完整 POS 快照。
- [ ] 將商品、銷售、結款、商品歷史拆成關聯式資料表，避免整套資料集中在一筆可覆寫 JSON。
- [x] 主資料每次變更前自動建立不可由前端修改或刪除的快照與事件紀錄。
- [x] 敏感操作記錄操作者帳號、角色、時間、動作、原因及變更前後資料。
- [x] 已開啟 Supabase Auth 資料庫稽核紀錄。
- [ ] 建立每日資料庫匯出與異地備份；正式使用前實際演練一次還原。
- [x] 寫入 RPC 使用預期版本時間戳與資料庫交易，衝突時拒絕覆蓋並要求重新載入。

## 應用程式與裝置安全

- [x] Next.js 升級至 16.3.3，全部相依套件 `npm audit` 為 0，TypeScript、Lint 與 production build 通過。
- [x] 加入 CSP、`frame-ancestors`／X-Frame-Options、X-Content-Type-Options、Referrer-Policy、Permissions-Policy。
- [x] Supabase 環境變數缺失或權限查詢失敗時拒絕存取。
- [x] 登出時清除舊 localStorage／sessionStorage POS 資料；正式資料只信任 Supabase。
- [x] 不再把完整供應商與財務資料長期保存在瀏覽器 localStorage。

## 完成標準

- [x] 未登入者無法讀取或修改任何 POS／供應商資料。
- [x] 自行註冊不可用；未在 `kc_app_members` 啟用的帳號無法進入正式系統。
- [x] 員工、管理員與供應商入口及資料權限分離。
- [x] 供應商 A 無法透過網址、API 或修改請求讀取供應商 B 的資料。
- [ ] 商品售出、銷帳與歷史紀錄無法重複執行或被一般帳號竄改。
- [x] 已執行相同資料快照還原演練；還原後仍為 18 件商品、5 位廠商、1 筆銷售、1 筆結款，且產生還原稽核。
- [x] Supabase Security Advisor 為 0 errors；唯一 warning 是 Free 方案無法啟用的外洩密碼防護，已記錄並使用其他密碼防護補強。
- [x] `npm audit`、TypeScript、Lint 與 production build 通過；主要操作流程待正式部署後驗證。
- [ ] 完成正式部署後，再檢查回應安全標頭與各角色實際權限。
